package com.syncpoint.compliance.evidence.entity;

/** Where the evidence originated (PROJECT_SPEC2 §21). */
public enum EvidenceSourceType {
    MANUAL_UPLOAD,
    GITHUB,
    AWS,
    JIRA,
    GOOGLE_WORKSPACE
}
