package com.syncpoint.compliance.evidence.entity;

/** Classification the LLM (or reviewer) assigns to a mapping (PROJECT_SPEC2 §35). */
public enum MappingClassification {
    COVERED,
    PARTIAL,
    INSUFFICIENT
}
